#include "ShaderTypes.h"

// ShaderTypes.h contains only data types and plain structs; there is no
// non-inline implementation to place here.  This translation unit exists
// so that the CMake target has at least one .cpp file and to give a natural
// home for any future utility functions that operate on these types.
